[tool.black]
line-length = 88
target-version = ["py311"]
#exclude = '''
#/path/to/dir/.*
#'''

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "IoTKafka"
version = "1.0.0"
description = "Kafka Consumer/Producer"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Iot Team Alpha" }
]
dependencies = ["confluent-kafka==2.12.1"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
shared_lib = ["py.typed"]